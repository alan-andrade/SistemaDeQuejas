- content_for :nav do
  = back_button tickets_path

%h1 Managers
// This is a quick implementation of the view. NEEDS to get optimized using heurisitcs or something else!

- for manager in @managers
  %h2= manager.name
  = manager.ticket_taker? ? "(Responsable)" : nil
  %p
    %i= manager.uid
  = button_to "Asignar como Responsable Principal", ticket_taker_path(manager.uid), :method=>:post
  %hr
